interface: 0.0.0.0
worker_threads: 4

file_roots:
  base:
    - /srv/salt/base

pillar_roots:
  base:
    - /srv/salt/pillar

fileserver_backend:
  - roots

reactor:
  - 'salt/minion/*/start':
    - salt://reactor/startup_tasks.sls
  - 'salt/beacon/*/diskusage':
    - salt://reactor/diskusage_write_to_file.sls

auto_accept: True
