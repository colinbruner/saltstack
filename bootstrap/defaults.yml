bootstrap:
  pkgs:
    - salt-master
    - python36
    - python36-devel
    - libgit2
    - libgit2-devel
  pip:
    pkgs:
      - pygit2
  files:
    master_config:
      src: salt://bootstrap/files/salt/configs/master
      dest: "/etc/salt/master"
